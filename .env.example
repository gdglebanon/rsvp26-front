# Leave blank for /rsvp-gdg/ locally and / in production.
# On Vercel set / explicitly; GitHub Pages can use /rsvp26-front/.
VITE_BASE_PATH=

# FastAPI backend. Firebase public web config is loaded from /api/config.
# On Vercel: https://rsvp26-back-ktwg.vercel.app (without /api).
VITE_API_URL=http://localhost:8000
