rules_version = '2';
service cloud.firestore {
 match /databases/{database}/documents {
  // All attendee data is served by the API after verified Firebase authentication.
  match /{document=**} { allow read, write: if false; }
 }
}
